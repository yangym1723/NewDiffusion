# 我在diffusion_policy目录下在robodiff_yym的conda环境中使用命令python train.py --confgi-name=train_diffusion_transformer_real_hybrid_workspace启动了我的模型训练代码，然后在训练出来模型后，在env_isaaclab_yym的conda环境中使用python eval_isaaclab.py -c data/outputs/2026.03.06/21.45.55_train_diffusion_transformer_hybrid_real_image/checkpoints/epoch=0560-train_loss=0.001.ckpt -o data/eval_output --task Template-Threefingers-v0 --enable_cameras --n_test 10 --n_test_vis 3来进行模型的测试，但是在模型测试时，机械臂运动到一个位置后发生了该报错：xxx，可是在我用同一个isaaclab仿真环境收集数据的时候，机械臂运动到这个位置是没有问题的，为什么现在就会在这个位置报错并且环境卡死崩溃呢？请详细思考后帮我解决这个问题并且把你的解决策略和代码改动详细的在diffusion_policy目录下创建一个.md文件写下来方便我阅览